# Copy this file to .env and set real values
# cp .env.example .env

# Required
VAPID_PUBLIC_KEY=YOUR_VAPID_PUBLIC_KEY

# Option A (recommended for local simplicity): PEM private key with escaped newlines
# Example: -----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----
VAPID_PRIVATE_KEY=

# Option B (alternative): base64-encoded PEM private key
# If this is set and VAPID_PRIVATE_KEY is empty, the app will use this value.
VAPID_PRIVATE_KEY_B64=

# Optional
VAPID_SUBJECT=mailto:pocketkid@example.com
